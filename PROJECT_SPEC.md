# 桑蚕原种卵卡入低温冷藏库联合检验闭环

## 项目目标

建设一个面向蚕种繁育站的 Go HTTP 后端，以“原种谱系—制种批次—卵卡封签”为任务主轴，在单一聚合流程内完成资料锁定、双人制种确认、样本封存、资源租约、逐日龄孵化观察、母蛾磨片镜检、卵面水分与破损复测、病原复判、双人独立复核和终局裁定。系统采用事务型持久化与启动恢复，所有外部仪器交互通过可重试调用记录驱动；并发资源竞争、证据版本链、操作幂等和终局单写均由存储层约束与聚合代次共同保证。实现规模限定为20至30个生产Go文件、2000至3000行有效生产Go代码，目标约2500行，分布在至少4个有真实职责的包中；公开测试不少于4个测试文件和12个确定性测试用例。

## 端到端业务流程

1. 建档与一次性锁定：依据目录校验原种谱系、制种批次和母蛾袋摘要，锁定卵卡封签、催青舱位、冷藏库格位、观察日龄、盲码、载玻片、各项阈值、复核名单及任务代次；任一唯一项冲突时整笔事务回滚。
2. 制种确认与样本封存：两名不同且具备制种资格的人员按当前代次确认；随后为每张卵卡登记三联样本、密封摘要和盲码，在同一事务内取得催青舱位及载玻片租约并进入观察流程。
3. 观察与仪器采集：按锁定日龄和卵卡形成覆盖矩阵，提交孵化卵、未孵卵、死卵、破损卵及补测标记；显微镜、温湿度探头和水分仪调用先持久化再执行，以逻辑时间记录成功、拒绝、断连、超时或格式错误及确定性重试。
4. 证据核验与复判：保存不可覆盖的母蛾磨片镜检、催青环境、卵面水分和破损复测版本；活力不足、死卵率越界、微粒子病疑阳或三联样本分歧仅能触发一个当前代次复判，旧代次结果留痕但不得参与裁定。
5. 独立复核与终局竞争：证据闭合后，两名互不相同且不得与制种确认角色违规重合的合格人员独立复核；入冷藏、病害隔离和取消通过同一终局单写屏障竞争，成功入冷藏时生成唯一凭据并确认冷藏格位占用。
6. 恢复与审计：服务重启后从数据库恢复开放任务、租约、覆盖矩阵、未完成仪器调用、复判代次及终局；过期租约仅能按规则回收，所有拒绝输出稳定错误码和确定排序的原因，失败事务不遗留局部状态。

## 核心组件与职责

1. 蚕种谱系与催青规则目录：维护带版本的谱系、制种批次、母蛾袋摘要、催青日龄、阈值、人员资格及角色互斥规则，并提供锁定快照校验。
2. 原种卵入冷藏任务聚合：实现十二种业务状态、代次推进、一次性锁定闸门、操作号幂等记录、状态转换和终局单写屏障。
3. 卵卡样本与资源占用账簿：管理卵卡封签、三联样本、盲码揭示，以及催青舱位、冷藏库格位和载玻片的唯一租约、换舱、续租与恢复。
4. 日龄孵化及理化采集账簿：维护日龄覆盖矩阵、计数守恒校验、定点整数计算、仪器调用尝试和不可覆盖理化证据版本链。
5. 微粒子病复判及终局仲裁器：判定复判触发条件与覆盖范围，隔离旧代次证据，检查双人独立复核并原子生成入冷藏、病害隔离或取消结论。
6. Go HTTP API：提供JSON接口、中间件、稳定错误协议和健康检查，将请求映射为原子应用命令；不引入独立前端或无关管理子系统。

## 领域规则与不变量

1. 状态严格沿待锁定、待制种确认、样本封存中、舱位占用中、孵化观察中、镜检核验中、理化复测中、待独立复核推进，并仅能终结为可入冷藏后已入冷藏、病害隔离或已取消；终态拒绝全部迟到业务写入。
2. 制种批次、卵卡封签、盲码、催青舱位、冷藏库格位和载玻片在开放任务中全局唯一；数据库唯一约束与事务锁共同裁定并发锁定、换舱和启动，不以进程内互斥锁作为唯一保障。
3. 锁定快照不可修改，必须包含目录版本、任务代次及所有指定实体和阈值；谱系或批次错配、母蛾袋摘要陈旧、封签重复、盲码重复或提前揭示均拒绝推进。
4. 所有变更命令携带操作号与规范化内容摘要；相同操作号和相同内容返回原结果，不同内容返回OPERATION_CONFLICT。涉及推进的命令还必须匹配当前任务代次。
5. 每个覆盖格由卵卡封签与日龄唯一确定；四类卵数必须非负且总和等于锁定样本数，重复日龄、缺失日龄和无补测授权的覆盖替换均无效。
6. 孵化率、死卵率、水分、温度、相对湿度和孢子计数使用明确小数位的有符号64位整数；解析、缩放、乘除前检查长度、符号、除零和溢出，比较采用交叉乘法或安全缩放且不使用浮点数。
7. 仪器失败只能追加待重试调用及尝试记录；重试沿用任务、样本、仪器、读数类型和代次，次数及下一逻辑时刻由固定策略决定，失败不得生成合格证据、揭盲或释放租约。
8. 镜检、理化与复判证据均为追加式版本链；每个触发集合在当前代次只有一个复判，复判必须覆盖受影响卵卡、盲码、日龄和载玻片，旧代次迟到结果仅供审计。
9. 制种确认者必须为两名不同人员；终局复核者也必须为两名不同合格人员，并遵守锁定的角色互斥名单。任一人员资格在锁定快照中判定，后续目录变化不追溯改变已锁定任务。
10. 拒绝响应包含稳定错误码、当前状态和原因数组；原因按原种谱系、制种批次、卵卡封签、孵化日龄、载玻片编号及原因码依次排序。任何失败事务不得残留样本、租约、覆盖格、盲码揭示、证据或终局。

## 数据模型与持久化

1. CatalogRule：谱系编号、规则版本、允许制种批次、催青日龄集合、各项定点阈值、样本量、角色资格和互斥规则。
2. StorageTask：任务ID、谱系、制种批次、母蛾袋及摘要、锁定快照、当前代次、业务状态、版本号、创建与逻辑更新时间。
3. EggCard与SampleSeal：卵卡封签、样本序号、三联分组、密封摘要、盲码、揭盲状态及所属任务；开放任务范围内设置业务唯一键。
4. ResourceLease：资源类型、资源编号、任务ID、代次、租约状态、取得时间、到期逻辑时间及释放原因；对有效租约建立条件唯一约束。
5. ObservationCell：任务、卵卡、日龄、孵化卵数、未孵卵数、死卵数、破损卵数、补测标记、提交者和证据版本。
6. EvidenceVersion：证据类型、任务、样本或卵卡、日龄、载玻片、代次、版本序号、定点读数、来源调用、校验状态和前序版本引用。
7. InstrumentCall与Attempt：仪器类型、目标对象、请求摘要、任务代次、调用状态、尝试次数、故障类别、下一重试逻辑时间和原始响应摘要。
8. RecheckCase：触发集合摘要、复判代次、受影响卵卡/盲码/日龄/载玻片清单、证据闭合状态和复判结论。
9. Confirmation与Review：操作角色、人员编号、任务代次、内容摘要、结论及逻辑时间；通过唯一键限制同角色重复提交。
10. OperationRecord与FinalDecision：操作号、规范内容摘要、稳定响应；终局类型、裁定版本、入冷藏凭据号、裁定者和逻辑时间，任务级终局唯一。

## 公开接口

1. POST /v1/tasks：创建待锁定任务；POST /v1/tasks/{id}/lock：提交完整锁定清单和目录期望版本。
2. POST /v1/tasks/{id}/production-confirmations 与 POST /v1/tasks/{id}/samples/seal：完成双人确认及三联样本原子封存。
3. POST /v1/tasks/{id}/leases/acquire、/move、/renew：取得或调整催青舱位、冷藏格位及载玻片租约。
4. POST /v1/tasks/{id}/observations：提交单个或批量日龄覆盖格；GET /v1/tasks/{id}/coverage：返回按卵卡和日龄排序的缺口。
5. POST /v1/tasks/{id}/instrument-calls 与 POST /v1/instrument-calls/{callID}/retry：驱动可脚本化的显微镜、探头和水分仪适配器；生产实现可使用HTTP适配器，测试使用内存故障脚本。
6. POST /v1/tasks/{id}/blind-codes/reveal、/evidence/verify 与 /rechecks：执行受控揭盲、证据核验及当前代次复判。
7. POST /v1/tasks/{id}/reviews 与 POST /v1/tasks/{id}/decisions/{admit|isolate|cancel}：提交独立复核并竞争终局；GET /v1/tasks/{id} 返回聚合状态、证据摘要和确定排序原因。
8. GET /healthz 与 GET /readyz：分别报告进程存活和数据库迁移、恢复扫描完成状态；所有写接口使用Operation-Id头并返回统一错误对象。

## 失败边界

1. 单个应用命令在一个数据库事务内写入聚合、幂等记录、样本、租约、覆盖格和证据；校验失败或提交冲突整体回滚。
2. 使用SQLite WAL作为默认真实持久化实现并抽象仓储边界；事务、唯一索引、乐观版本号和忙重试次数固定，保证单进程多协程公开测试可重复，服务重启后执行确定顺序恢复扫描。
3. 外部仪器调用不得与数据库事务假定原子；采用持久化调用箱，先提交调用意图，再执行适配器并以调用ID幂等落库，崩溃后可继续同一调用而不伪造读数。
4. 并发竞争以数据库结果为准：唯一约束冲突映射为稳定业务错误，聚合版本冲突按固定上限重试，耗尽后返回CONCURRENT_MODIFICATION。
5. 整数解析、缩放、求和、乘除或阈值比较出现格式、符号、除零或溢出时返回ARITHMETIC_INVALID，且不写入派生证据。
6. 终局更新以任务非终态条件和任务级唯一终局记录为屏障；竞争失败者读取并返回既有终局，若请求类型不同则返回FINAL_DECISION_CONFLICT。
7. HTTP层限制请求体大小和数组长度，拒绝未知字段；内部错误仅返回稳定INTERNAL_ERROR及追踪号，详细原因写入审计日志。

## 验收标准

1. 锁定成功后能精确读取固定的谱系、制种批次、母蛾袋、卵卡、封签、催青舱位、冷藏格位、日龄、盲码、载玻片、全部阈值、复核名单与任务代次；陈旧目录或错配数据整笔失败。
2. 并发创建、锁定、换舱或启动时，每个批次、封签、盲码、催青舱位、冷藏格位和载玻片最多属于一个开放任务；失败方无样本或租约残留。
3. 所有代次敏感命令拒绝陈旧代次；相同操作号同内容可重复取得原响应，不同内容稳定报冲突，角色重合无法完成制种确认或独立复核。
4. 全部锁定卵卡和日龄必须形成守恒的活力覆盖矩阵；缺失、重复、负数、总数不符及未授权补测不产生有效覆盖格。
5. 所有比率与温湿度、水分、孢子读数只用定点整数完成边界判定，并覆盖最大值、负号、超长输入、除零与溢出；算术失败不生成证据。
6. 三类仪器的拒绝、断连、超时和格式错误均形成可恢复调用记录；在逻辑时钟推进后按确定次数重试，服务重启仍能继续且不提前揭盲、放行或释放资源。
7. 异常条件仅创建一个当前代次复判并完整覆盖受影响对象；旧代次迟到读数保持可审计且不改变当前证据链或最终判断。
8. 证据全部闭合且两名合格人员独立复核后，入冷藏、隔离和取消并发竞争只产生一个终局；合格路径生成唯一入冷藏凭据，任何终态后写入均被拒绝且数据库状态不变。

## 确定性测试场景

1. catalog_lock_test.go：成功锁定完整快照；谱系与制种批次错配；母蛾袋摘要陈旧；重复封签或盲码导致全事务回滚。
2. lease_concurrency_test.go：使用同步屏障让两个任务竞争同一催青舱位、冷藏格位和载玻片；验证仅一方成功；并发换舱原子性；重启后有效租约仍阻止抢占。
3. operation_generation_test.go：同操作号同内容幂等；同操作号异内容冲突；陈旧任务代次拒绝；制种确认者与复核者角色重合拒绝。
4. observation_math_test.go：完整多日龄覆盖；重复或缺失观察；四类计数不守恒；补测覆盖规则；孵化率与死卵率恰好落在阈值两侧。
5. fixedpoint_test.go：温度、相对湿度、水分和孢子读数的定点解析与比较；超长、负值、除零、加乘溢出均返回精确错误且无证据写入。
6. instrument_retry_test.go：用逻辑时钟和故障脚本复现拒绝、断连、超时、格式错误后成功；断言尝试序号、下一重试时刻、调用对象及重启恢复一致。
7. recheck_version_test.go：活力不足、死卵越界、疑阳和三联分歧合并为唯一复判；覆盖对象完整；旧代次镜检迟到结果不影响新代次结论。
8. final_race_test.go：两名复核完成后的入冷藏竞争只生成一个凭据；入冷藏、隔离和取消三方同步竞争仅有一个终局；终态后观察、复判和复核均无状态变化。

## 组件追踪关系

1. 目录组件对应锁定快照、陈旧规则、谱系批次匹配、阈值与人员资格规则，主要由catalog_lock_test.go验证。
2. 任务聚合对应十二状态、代次、操作幂等和终局屏障，主要由operation_generation_test.go与final_race_test.go验证。
3. 样本与资源账簿对应封签、盲码、三联样本及三类资源并发唯一租约，主要由lease_concurrency_test.go验证。
4. 孵化及理化账簿对应日龄覆盖、计数守恒、定点整数运算、仪器调用与重启恢复，主要由observation_math_test.go、fixedpoint_test.go和instrument_retry_test.go验证。
5. 复判及仲裁器对应疑阳触发、复判代次隔离、双人复核、唯一凭据及三类终局竞争，主要由recheck_version_test.go与final_race_test.go验证。
6. HTTP API对应输入限制、Operation-Id、统一错误结构和原因排序；各测试除直接领域测试外，至少包含一组httptest端到端精确JSON断言。

## 独特性

该计划专注于桑蚕原种卵卡入低温冷藏前的联合检验，把母蛾袋、三联盲样、按催青日龄推进的孵化覆盖矩阵、微粒子病磨片复判和冷藏格位终局凭据组合为同一持久化状态机；其资源模型、证据链和病害隔离逻辑均源于蚕种繁育业务，不复用既有种蛋、作物种子或食品批次放行项目的名称与流程。
